#include<stdio.h>
#define pi 3.14
void main(){
float radii, area ,circum;
radii=10;
area = pi*radii*radii;
circum=2*pi*radii;
printf("Area of circle with %f radius is %f\n",radii,area);
printf("Circumference of circle with %f radius is %f\n",radii,circum);

}
